# ProofRail — Gold Release Index

Per-release reference in compact form. One entry per Gold release.
Each entry covers: scope, key tools, schemas / docs, regression test,
manifest shape, key non-claims.

Gold-tier work began at v0.4.0 after the Silver tier closed at v0.3.7.
For Bronze and Silver releases, see `docs/dev/silver-release-index.md`.
For per-release command invocations, see `docs/dev/silver-command-index.md`.
For durable process rules, see `docs/dev/coding-assistant-guardrails.md`.
For long-form release narratives, see `docs/gold/gold-<slug>-v0.4.x.md`
and `docs/gold/gold-boundary-v0.2.5.md`. For the current operating
release, see `CLAUDE.md`.

## Cross-cutting Gold design patterns

- **Deterministic hashing:** Canonical JSON `json.dumps(obj, sort_keys=True, separators=(",",":"))` ensures reproducible SHA256 hashes.
- **Hash-first re-anchoring:** every mutation to a subject is followed by a `rehash_subject` call that re-anchors the manifest's subject sha256 and the cross-anchor fields.
- **Staging-then-replace:** stage under `<output-dir>.staging.<pid>` and `os.replace()` only after successful `--self-validate`.
- **Subprocess delegation:** later Gold runners subprocess earlier Gold runners into tempdirs; later Gold verifiers subprocess earlier Gold verifiers on synthesized lower-rank manifests assembled in tempdirs. Inherited verifier failures are relayed unchanged with no wrapper reason. Environmental failures (missing or crashing co-located lower-rank verifier) surface as non-reason-shaped `INFRA:` diagnostics.
- **Closed reason surface:** every Gold release declares a closed verifier reason set and a closed runner-only refusal set; cases match one specific reason; runner emits only runner-only refusals, verifier emits only verifier reasons.
- **TG1 + scoped SS snapshot:** every Gold release ships a hard TG1 taxonomy gate over its own owned files and a scoped sha256 snapshot of its own owned files. Inherited tokens from prior Gold releases that appear in the new release's source are allowed via an explicit, narrowly-scoped allowlist.

---

## Gold v0.4.0

- **Demo:** `demos/gold-demo-001-governed-reliance/`. Runtime under `/tmp/proofrail-gold-governed-reliance-v0.4.0/`.
- **Tools:** `build_gold_governed_reliance_demo_v0_1_0.py` (5 runner-only refusal codes; deterministic 24-entry conformance report; cross-anchored `package_id` and `governed_reliance_demo_id`); `verify_gold_governed_reliance_demo_v0_1_0.py` (24 stable reasons across 24 ordered structural checks plus post-structural conformance-report re-derivation; never emits runner-only codes).
- **Test:** `test_gold_governed_reliance_v0_4_0.sh` (53 exercises: 4 PP + 5 SC + 24 case + 11 dup + 6 runner-only refusal exercises covering 5 distinct runner-only reasons (ro1, ro2, ro2b, ro3..ro5) + 1 rel + 1 TG + 1 SS). TG1 ships with EMPTY allowlist.
- **Manifest:** 2 subjects (`governed-reliance-scenarios.json`, `silver-gold-governed-reliance-conformance-report.json`) cross-anchored by `package_id` and `governed_reliance_demo_id`.
- **Inputs model — 5 closed Silver-shaped inputs:** `silver_verification`, `silver_handoff`, `policy_pack`, `registry_lite`, `control_crosswalk`. Each is a closed input-type + input-ref pointer; v0.4.0 does NOT re-verify upstream Silver evidence chains.
- **Governed decision model — 5 closed scenarios:** `clean_acceptance`, `policy_rejection`, `challenge_filed`, `withdrawal`, `supersession`. 1..5 entries per body, unique `scenario_type`, natural-order placement. Each entry binds `decision_subject` (3 closed subject types), `policy_binding` (5 closed policy_decision values), `registry_binding` (3 closed authority roles), `action_scope` (5 closed action ids drawn from v0.3.6 catalog), `decision_status` under closed mapping, `scenario_specific_state` keyed on `scenario_type`, and `decision_trigger` under closed enum.
- **Supersession reference kinds:** `internal_decision_id` (resolves inside the package's `governed_decisions[]`) or `external_decision_id` (prior decision outside this package; the verifier does not require resolution).
- **Fixture variants (6):** `governed-reliance-scenarios.json` (canonical 5-scenario body, uses `internal_decision_id`), 5 single-scenario slices for `clean_acceptance` / `policy_rejection` / `challenge_filed` / `withdrawal` / `supersession` (the supersession slice uses `external_decision_id`).
- **Overclaim guard:** closed prohibited Gold-claim vocabulary scanning every string value outside `scope_limitations` and `non_claims` for tokens including `signed gold reliance`, `gold reliance certificate`, `gold certificate`, `gold accepted`, `full gold`, `federated reliance`, `transferred reliance`, `regulator approval`, `auditor approval`, `audit ready`, `legally enforceable`, `production authorized`, `production pki`, `certificate authority`, `certification authority`, `control operating effective`, `runtime truth`.
- **Bundled-report disagreement OR `package_id` / `governed_reliance_demo_id` cross-anchor mismatch:** funneled to `gold_manifest_invalid` (no 25th reason).
- **Path traversal:** ro2 + ro2b both emit `runner_input_path_forbidden` (absolute + parent-traversal variants).
- **Reference:** `docs/gold/minimal-gold-governed-reliance-v0.4.0.md`.
- **Non-claims:** Unsigned; not a certificate; not federated; not a transfer of reliance to any external party; does not claim regulator approval, auditor approval, legal acceptance, legal adjudication, legal enforceability, compliance certification, production authorization, production governance, production PKI, audit readiness, or control operating / design effectiveness; does not consult any live service, gateway, observability backend, policy engine, GRC platform, or external registry; does not perform end-to-end re-verification of the upstream Silver evidence chain; not full Gold; not Platinum; does not represent runtime truth.

---

## Gold v0.4.1

- **Demo:** `demos/gold-demo-002-decision-report-hardening/`. Runtime under `/tmp/proofrail-gold-decision-report-hardening-v0.4.1/`.
- **Scope:** narrow Gold maintenance release — Gold Decision Report Hardening. Re-projects the unchanged v0.4.0 governed-reliance package body into a deterministic local Gold decision report, paired with the v0.4.0 conformance report and bound by a 3-subject manifest.
- **Architecture:** v0.4.1 runner subprocesses the unchanged v0.4.0 runner to produce subjects [0] and [1] in a tempdir, then byte-copies them under the staging directory and derives the v0.4.1 decision report as subject [2]; v0.4.1 verifier subprocesses the co-located v0.4.0 verifier on a synthesized 2-subject v0.4.0 manifest and adds five v0.4.1-owned structural checks over the decision report; staging-then-replace; relays verifier failures unchanged on `--self-validate`.
- **Test:** `test_gold_decision_report_hardening_v0_4_1.sh` (61 exercises). TG1 ships with a closed five-entry allowlist limited to exact `coverage_summary` data-field names and an environmental-wrapper deny-list constructed via string concatenation to prevent self-trip.
- **Reference:** `docs/gold/gold-decision-report-hardening-v0.4.1.md` holds the full reason surface (24 inherited + 5 v0.4.1-owned), reachability orderings, identifier-grammar ownership split, subprocess-delegation architecture, TG1 allowlist discipline, and non-claims.
- **Non-claims:** Unsigned; not a new Gold tier; not a certificate; not federated; not a transfer of reliance to any external party; does not claim regulator approval, auditor approval, legal acceptance, legal adjudication, legal enforceability, compliance certification, production authorization, production governance, production PKI, audit readiness, or control operating / design effectiveness; does not consult any live service; does not perform end-to-end re-verification of the upstream Silver evidence chain; does not extend the substance of the v0.4.0 body; not full Gold; not Platinum; does not represent runtime truth.

---

## Gold v0.4.2

- **Demo:** `demos/gold-demo-003-policy-evaluation-matrix/`. Runtime under `/tmp/proofrail-gold-policy-evaluation-matrix-v0.4.2/`.
- **Scope:** narrow Gold maintenance release — Gold Policy Evaluation Matrix. Pairs the unchanged v0.4.0 governed-reliance package body and v0.4.1 decision report with a deterministic local policy evaluation matrix and a byte-re-derivable policy evaluation report, bound by a 5-subject manifest cross-anchored to the v0.4.0 body and to the v0.4.1 decision report.
- **Architecture:** v0.4.2 runner subprocesses the v0.4.1 runner into a tempdir (which itself subprocesses the v0.4.0 runner), byte-copies subjects [0], [1], and [2] into the v0.4.2 staging directory, injects two runtime-bound scalars (`decision_report_sha256` anchor and `generated_at` ISO-8601 UTC timestamp) to derive subject [3], folds subject [3] against subject [2] to derive subject [4] and the 5-subject manifest, runs the v0.4.2 verifier under `--self-validate`, and atomically `os.replace()`s into the destination. v0.4.2 verifier subprocesses the co-located v0.4.1 verifier on a synthesized 3-subject v0.4.1 manifest assembled in a tempdir from subjects [0], [1], and [2], and adds nine v0.4.2-owned structural checks; relays inherited v0.4.0 / v0.4.1 failures unchanged; environmental failures surface under a non-reason-shaped `INFRA:` diagnostic.
- **Matrix shape split:** committed fixture is a Matrix Template (canonical row set + natural order); Runtime Matrix is the template plus the two injected runtime scalars, byte-re-derived and re-anchored at build time. The schema separates the two shapes explicitly so downstream tooling can verify either.
- **Test:** `test_gold_policy_evaluation_matrix_v0_4_2.sh` (78 exercises: 6 PP + 38 canonical (24 inherited from v0.4.0 + 5 inherited from v0.4.1 + 9 v0.4.2-owned) + 3 runtime-scalar canonicals + 18 dup + 4 supplementals + 6 runner-only refusal exercises covering 5 distinct runner-only reasons + 1 rel01 + 1 rel02 + 1 env01 INFRA diagnostic + 1 TG1 + 1 SS). TG1 ships with a closed five-entry allowlist limited to the five inherited v0.4.1 `coverage_summary` data-field names and no v0.4.2-introduced data-field names; environmental-wrapper deny-list constructed via string concatenation to prevent self-trip.
- **Manifest:** 5 subjects (`governed-reliance-scenarios.json`, `silver-gold-governed-reliance-conformance-report.json`, `gold-governed-reliance-decision-report.json`, `gold-policy-evaluation-matrix.json`, `gold-policy-evaluation-report.json`) cross-anchored by `package_id` and `governed_reliance_demo_id` to the v0.4.0 body, by `conformance_report_id` and `decision_report_id` to the v0.4.1 surface, and by v0.4.2-owned `matrix_id` and `policy_evaluation_report_id`. Runtime matrix cross-anchors `decision_report_sha256` to subject [2]; evaluation report cross-anchors `source_decision_report_sha256` and `source_matrix_sha256` to subjects [2] and [3].
- **Reference:** `docs/gold/gold-policy-evaluation-matrix-v0.4.2.md` holds the full reason surface (24 inherited from v0.4.0 + 5 inherited from v0.4.1 + 9 v0.4.2-owned), reachability orderings, subprocess-delegation chain, TG1 allowlist discipline, INFRA diagnostic boundary, Matrix Template vs Runtime Matrix split, and non-claims.
- **Non-claims:** Unsigned; not a new Gold tier; not a certificate; not federated; not a transfer of reliance to any external party; not a policy engine, GRC platform, gateway, SIEM, observability backend, certification authority, regulator, or production authorization system; does not consult any live policy engine or external registry; does not perform end-to-end re-verification of the upstream Silver evidence chain; does not extend the substance of the v0.4.0 body or the v0.4.1 decision report; not full Gold; not Platinum; does not represent runtime truth.

---

## Gold v0.4.3

- **Demo:** `demos/gold-demo-004-challenge-lifecycle-lite/`. Runtime under `/tmp/proofrail-gold-challenge-lifecycle-lite-v0.4.3/`.
- **Scope:** narrow incremental Gold release — Gold Challenge Lifecycle Lite. Pairs the unchanged v0.4.0 governed-reliance package body, the unchanged v0.4.1 decision report, and the unchanged v0.4.2 policy evaluation matrix and policy evaluation report with a hand-authored deterministic local runtime challenge-lifecycle records body (one lifecycle record per governed decision row in natural order) and a deterministic local lifecycle report (row-per-record projection plus a closed-vocabulary `coverage_summary` keyed on the six lifecycle status values), bound by a 7-subject manifest cross-anchored to the v0.4.0 body, v0.4.0 conformance report, v0.4.1 decision report, v0.4.2 matrix, v0.4.2 evaluation report, v0.4.3 records body, and v0.4.3 lifecycle report.
- **Architecture:** v0.4.3 runner subprocesses the unchanged v0.4.2 runner into a tempdir (which itself subprocesses the v0.4.1 and v0.4.0 runners), byte-copies subjects [0]..[4] into the v0.4.3 staging directory, reads the hand-authored records template at `--lifecycle-input` and injects three runtime-bound scalars (top-level `policy_evaluation_report_sha256` anchor to subject [4], top-level `generated_at` ISO-8601 UTC, per-record `lifecycle_fingerprint` bare lowercase hex SHA-256 over canonical JSON of each record body excluding the fingerprint field) to derive subject [5], folds subject [5] against subjects [4] and [2] to derive subject [6] (one row per lifecycle record with a closed projection of `row_id`, `lifecycle_id`, `target_decision_id`, `target_decision_row_id`, `current_status`, `is_terminal`, `event_count`, `first_event_id`, `final_event_id`, `final_event_timestamp`, and `lifecycle_fingerprint`) and the 7-subject manifest, runs the v0.4.3 verifier under `--self-validate`, and atomically `os.replace()`s the staging directory into the destination. v0.4.3 verifier subprocesses the co-located v0.4.2 verifier on a synthesized 5-subject v0.4.2 manifest assembled in a tempdir from subjects [0]..[4]; the v0.4.2 verifier's 38 inherited reasons surface only on failure (relayed verbatim, no v0.4.3 wrapper). v0.4.3 adds ten v0.4.3-owned structural checks (R39..R48). Environmental failures (missing or unlaunchable co-located v0.4.2 verifier) surface under a non-reason-shaped `INFRA:` diagnostic.
- **Records-body shape split:** committed fixture is a Records Template (canonical 5-record set + natural order keyed by `target_decision_id`); Runtime Records Body is the template plus the three injected runtime scalars (top-level `policy_evaluation_report_sha256`, top-level `generated_at`, per-record `lifecycle_fingerprint`), byte-re-derivable and re-anchored at build time. The schema (`schemas/gold-challenge-lifecycle-records-v0.1.0.md`) separates the two shapes explicitly.
- **Closed lifecycle vocabularies:** 6 `current_status` values (`filed`, `acknowledged`, `under_review`, `resolved_locally`, `superseded`, `withdrawn`); 6 `event_status` values (mirrors `current_status`); 6 `event_basis` values (`challenge_record`, `acknowledgement_record`, `review_update`, `local_resolution_record`, `supersession_record`, `withdrawal_record`); 4 `actor_role` values (`relying_party`, `policy_authority`, `reviewer`, `system_recorder`); 4 `lifecycle_effect` values (`challenge_open`, `local_resolution_recorded`, `challenge_withdrawn`, `challenge_superseded`). The closed status-transition graph permits 9 transitions: `filed → acknowledged`, `filed → withdrawn`, `filed → superseded`, `acknowledged → under_review`, `acknowledged → withdrawn`, `acknowledged → superseded`, `under_review → resolved_locally`, `under_review → withdrawn`, `under_review → superseded`. First event MUST be `filed`; no events MAY follow a terminal status; `resolved_locally` requires `local_resolution_ref`, `withdrawn` requires `withdrawal_record_ref`, `superseded` requires `superseding_decision_id`. `event_timestamp` is strictly non-decreasing within a record's `events` array (R43 enforcement).
- **Lifecycle-report coverage_summary (closed 5-field set, v0.4.3-owned):** `lifecycle_record_count`, `lifecycle_event_count`, `open_lifecycle_count`, `terminal_lifecycle_count`, `status_value_count` (closed 6-key map keyed on the six `current_status` values). Top-level `report_fingerprint` is a separate field (bare lowercase hex SHA-256 over canonical JSON of the report body excluding the fingerprint field itself), not a `coverage_summary` member.
- **Test:** `test_gold_challenge_lifecycle_lite_v0_4_3.sh`. v0.4.3 shipped with 99 exercises: 6 PP + 48 canonical (24 inherited from v0.4.0 + 5 inherited from v0.4.1 + 9 inherited from v0.4.2 + 10 v0.4.3-owned) + 4 runtime-scalar mutation variants folding to records-shape and records-binding reasons + 23 dup/subject-table/collision + 5 supplementals (R42 ×1, R43 ×3, R46 ×1) + 6 runner-only refusal exercises covering 5 distinct runner-only reasons + 1 runner-relay-of-verifier + 1 verifier-relay-of-inherited + 1 env01 INFRA diagnostic + 1 positive-determinism re-run + 1 no-residue assertion + 1 TG1 + 1 SS. The v0.4.3.1 corrective harness raises this to 101 by appending rt4 (R41 records-body `lifecycle_fingerprint` re-derivation) and sup06 (R47 lifecycle-report `report_fingerprint` re-derivation), exercising the deliberate non-monotonic fingerprint sub-checks added by the v0.4.3.1 verifier correction; the 99/99 reachability surface from v0.4.3 is preserved verbatim. TG1 ships with a closed five-entry allowlist limited to the inherited v0.4.1 decision-report `coverage_summary` enum-presence data-field names (`decision_statuses_present`, `policy_decisions_present`, `protected_actions_present`, `registry_roles_present`, `scenario_types_present`); no v0.4.3-introduced data-field names are allowlisted (v0.4.3 coverage-summary names do not satisfy the gate's reason-shaped suffix filter). Environmental-wrapper deny-list constructed via string concatenation to prevent self-trip. Test scratch path is `tests/_tmp_gold_challenge_lifecycle_lite_v0_4_3/` (test-owned; fail-safe pre-existence check, EXIT-trap cleanup, explicit no-residue assertion before SS-AFTER). No test artifact is ever written under an inherited-tier fixture directory.
- **Identifier grammar ownership (6-ID pairwise-distinctness collision class):** `conformance_report_id`, `decision_report_id`, `matrix_id`, `policy_evaluation_report_id`, `challenge_lifecycle_record_set_id`, `challenge_lifecycle_report_id`. v0.4.3 owns the last two grammars; the other four are inherited verbatim. All pairwise collisions across the 6-ID class surface at the manifest-integrity layer under `gold_manifest_invalid` (R01); the v0.4.3-owned `*_records_binding_invalid` (R41) and `*_report_binding_invalid` (R46) are reserved for body-level cross-anchor mismatches.
- **Manifest:** 7 subjects (`governed-reliance-scenarios.json`, `silver-gold-governed-reliance-conformance-report.json`, `gold-governed-reliance-decision-report.json`, `gold-policy-evaluation-matrix.json`, `gold-policy-evaluation-report.json`, `challenge-lifecycle-records.json`, `gold-challenge-lifecycle-report.json`) cross-anchored by `package_id` and `governed_reliance_demo_id` to the v0.4.0 body, by `conformance_report_id` and `decision_report_id` to the v0.4.1 surface, by `matrix_id` and `policy_evaluation_report_id` to the v0.4.2 surface, and by v0.4.3-owned `challenge_lifecycle_record_set_id` and `challenge_lifecycle_report_id`. Runtime records body cross-anchors `policy_evaluation_report_sha256` to subject [4]; lifecycle report cross-anchors `source_records_sha256`, `source_policy_evaluation_report_sha256`, and `source_decision_report_sha256` to subjects [5], [4], and [2] respectively.
- **Reachability orderings:** records body: not-object (R39) → schema-shape (R40) → binding (R41) → per-event validity (R42) → sequence/graph validity (R43) → deliberate post-R43 R41 sub-check (per-record `lifecycle_fingerprint` re-derivation; non-monotonic in the public-token integer suffix). Lifecycle report: not-object (R44) → schema-shape (R45) → binding (R46) → projection (R47) → summary (R48) → deliberate post-R48 R47 sub-check (top-level `report_fingerprint` re-derivation; non-monotonic). The non-monotonic placement preserves reachability of R42, R43, R47-row-level, and R48 on unmodified-fingerprint inputs without introducing new reasons. All inherited orderings (v0.4.0/v0.4.1/v0.4.2) precede the v0.4.3-owned orderings via subprocess relay.
- **Runner refusals (5, verbatim from v0.4.0, no sixth):** `runner_input_path_missing`, `runner_input_path_forbidden`, `runner_input_file_missing`, `runner_input_read_failed`, `runner_input_json_invalid`. Phase A preflight extends to all three external inputs (`--input-package`, `--matrix-input`, `--lifecycle-input`).
- **Reference:** `docs/gold/gold-challenge-lifecycle-lite-v0.4.3.md` holds the full reason surface (24 inherited from v0.4.0 + 5 inherited from v0.4.1 + 9 inherited from v0.4.2 + 10 v0.4.3-owned), reachability orderings, subprocess-delegation chain, TG1 allowlist discipline, INFRA diagnostic boundary, Records Template vs Runtime Records Body split, closed lifecycle vocabularies, test scratch path policy, and non-claims.
- **Non-claims:** Unsigned; not a new Gold tier; not a certificate; not federated; not a transfer of reliance to any external party; not a policy engine, GRC platform, gateway, SIEM, observability backend, certification authority, regulator, lifecycle adjudication authority, or production authorization system; does not consult any live policy engine or live lifecycle adjudication authority; does not perform end-to-end re-verification of the upstream Silver evidence chain; does not extend the substance of the v0.4.0 body, the v0.4.1 decision report, or the v0.4.2 policy-evaluation pair; not a signed lifecycle attestation; not live lifecycle adjudication; not full Gold; not Platinum; does not represent runtime truth.

---

## Gold v0.4.3.1

- **Scope:** narrow corrective patch over v0.4.3. Corrects verifier under-enforcement of two fingerprint sub-checks declared in the v0.4.3 narrative and schemas but not re-derived by the v0.4.3 shipped verifier.
- **Correction:** the v0.4.3.1 verifier now recomputes the per-record `lifecycle_fingerprint` (records body) and the top-level `report_fingerprint` (lifecycle report) over canonical JSON of the surrounding body excluding the fingerprint field itself. Mismatch on the records-body sub-check folds into existing `gold_challenge_lifecycle_records_binding_invalid` (R41) as a deliberate non-monotonic post-R43 sub-check; mismatch on the report sub-check folds into existing `gold_challenge_lifecycle_projection_invalid` (R47) as a deliberate non-monotonic post-R48 sub-check.
- **Closed surfaces unchanged:** verifier surface remains R01..R48 (24 inherited from v0.4.0, 5 from v0.4.1, 9 from v0.4.2, 10 v0.4.3-owned); no 49th reason; no 25th v0.4.0 reason; no sixth v0.4.1 reason; no tenth v0.4.2 reason; no eleventh v0.4.3 reason; no new wrapper reason for verifier-relay on `--self-validate`. Runner-only refusals remain the five inherited from v0.4.0. Closed lifecycle vocabularies, the 7-subject manifest shape, the subprocess delegation chain, and the TG1 allowlist discipline are untouched.
- **Test:** `tests/test_gold_challenge_lifecycle_lite_v0_4_3.sh` now reports 101/101 (the v0.4.3 99/99 reachability surface is preserved verbatim, plus `rt4` (R41 records-body `lifecycle_fingerprint` re-derivation) and `sup06` (R47 lifecycle-report `report_fingerprint` re-derivation)).
- **Tag preservation:** the v0.4.3 annotated tag is preserved unchanged. v0.4.3.1 is a forward-only corrective patch on `main`.
- **Reference:** `docs/releases/2026-06-26-proofrail-v0.4.3.1-release-note.md`.
- **Non-claims:** identical to v0.4.3. v0.4.3.1 corrects a verifier under-enforcement only and does not change the Gold boundary, the closed reason surface, or any v0.4.3 non-claim.

---

## Gold v0.4.4

- **Demo:** `demos/gold-demo-005-reliance-package-index/`. Runtime under `/tmp/proofrail-v044-reliance-package-index-demo/`.
- **Scope:** narrow incremental Gold release — Gold Reliance Package Index. Wraps the unchanged v0.4.0 Governed Reliance Demo, v0.4.1 Decision Report Hardening, v0.4.2 Policy Evaluation Matrix, and v0.4.3 Challenge Lifecycle Lite (under the corrected v0.4.3.1 verifier baseline) under a single 5-subject index manifest and a single byte-stable v0.4.4-owned index body. Does not introduce a new Gold tier, is not signed, is not federated, is not a registry, and does not extend the substance of any inherited release.
- **Architecture:** v0.4.4 runner subprocess-invokes each of the four co-located inherited runners (v0.4.0, v0.4.1, v0.4.2, v0.4.3) once, into its own `child-packages/v0.4.X/` subdirectory under the v0.4.4 staging tree, where each inherited runner writes its complete child closure (its wrapping manifest plus every subject file the manifest references); the v0.4.3 child closure is built under the corrected v0.4.3.1 baseline. The v0.4.4 runner then projects the seven v0.4.4 collision-class identifiers into the wrapping manifest, derives the v0.4.4 index body deterministically from the materialized child wrapping manifests (per-entry `child_manifest_fingerprint` / `child_manifest_size_bytes` / `child_manifest_path` byte-match the wrapping manifest's `subjects[i]`; closed-key `coverage_summary` arithmetic; top-level `index_fingerprint` over canonical JSON of the body excluding the fingerprint field), runs the v0.4.4 verifier under `--self-validate`, and atomically `os.replace()`s the staging directory into the destination. The v0.4.4 verifier validates the wrapping manifest (Phase 1) and wrapping-manifest cross-anchors (Phase 2), validates the v0.4.4-owned index body under six structural checks in the fixed order R49 → R50 → R52 → R53 → R51 → R54 (Phase 3), then subprocess-invokes each of the four co-located inherited verifiers (v0.4.0, v0.4.1, v0.4.2, v0.4.3) on its corresponding child wrapping-manifest path under `child-packages/v0.4.X/` (Phase 4). Inherited reasons R02..R48 are relayed verbatim with no v0.4.4 wrapper. Environmental failures of any inherited verifier (missing, unlaunchable, or non-FAIL non-zero exit) surface under a non-reason-shaped `INFRA:` diagnostic with exit code 3.
- **Input file chain (inherited, no v0.4.4-introduced input template):** `--input-package` (v0.4.0 governed-reliance scenarios), `--matrix-input` (v0.4.2 policy-evaluation-matrix template), `--lifecycle-input` (v0.4.3 challenge-lifecycle-records template). The v0.4.4 runner adds no `--index-input` flag; the v0.4.4 index body is fully derived.
- **7-ID pairwise-distinctness collision class (v0.4.4-owned invariant):** `conformance_report_id`, `decision_report_id`, `matrix_id`, `policy_evaluation_report_id`, `challenge_lifecycle_record_set_id`, `challenge_lifecycle_report_id`, `gold_reliance_package_index_id`. v0.4.4 owns the grammar of the last identifier; the other six are inherited. All 21 pairwise collisions across the class surface at the manifest-integrity layer under `gold_manifest_invalid` (R01); the v0.4.4-owned `gold_reliance_package_index_binding_invalid` (R51) is reserved for index-body-level cross-anchor mismatches. `manifest_id`, `package_id`, and `governed_reliance_demo_id` are explicitly excluded from the collision class.
- **Manifest:** 5 subjects in fixed order — (0) `gold_governed_reliance_package_manifest` at `child-packages/v0.4.0/gold-governed-reliance-package-manifest.json`; (1) `gold_decision_report_package_manifest` at `child-packages/v0.4.1/gold-decision-report-package-manifest.json`; (2) `gold_policy_evaluation_matrix_package_manifest` at `child-packages/v0.4.2/gold-policy-evaluation-matrix-package-manifest.json`; (3) `gold_challenge_lifecycle_package_manifest` at `child-packages/v0.4.3/gold-challenge-lifecycle-package-manifest.json`; (4) `gold_reliance_package_index` at `gold-reliance-package-index.json`. Cross-anchored by `package_id` and `governed_reliance_demo_id` to every child wrapping manifest and to the index body; by the six inherited collision-class IDs to their matching child wrapping manifest fields; by the v0.4.4-owned `gold_reliance_package_index_id` to the index body. The wrapping manifest does NOT include a self-referential manifest-hash subject; v0.4.4 ships local hash anchors only. Closure files under each `child-packages/v0.4.X/` subdirectory are NOT enumerated as v0.4.4 subjects and have no v0.4.4-owned reason coverage; their integrity is enforced by the inherited verifier's existing reasons via subprocess relay.
- **Index body:** `gold-reliance-package-index.json` (subject [4]) is the only v0.4.4-authored payload. Carries `document_type`, `schema_version`, `proofrail_release`, `hash_algorithm`, `gold_reliance_package_index_id`, `package_id`, `governed_reliance_demo_id`, `generated_at`, exactly four fixed-order `entries[]` (per inherited release), a closed-key `coverage_summary` (`child_package_count = 4`, `inherited_release_count = 4`, `pairwise_distinct_id_count = 7`, `package_id_anchor_consistency = true`, `governed_reliance_demo_id_anchor_consistency = true`), and a top-level `index_fingerprint` (bare lowercase hex SHA-256 over canonical JSON of the body excluding the fingerprint field). Stray keys at the top-level, per-entry, or `coverage_summary` layer surface under R50.
- **Reachability orderings:** Phase 1 manifest integrity (path-traversal before exact path equality, subject count == 5, subject role/order, identifier grammar). Phase 2 wrapping-manifest cross-anchors (binding anchors and the six inherited collision-class IDs; 21 pairwise collisions in the 7-ID class all fold into R01). Phase 3 index body, fixed v0.4.4 order R49 → R50 → R52 → R53 → R51 → R54. Phase 4 inherited verifiers in fixed order v0.4.0 → v0.4.1 → v0.4.2 → v0.4.3 (under the corrected v0.4.3.1 baseline), all inherited reasons relayed verbatim. R49..R54 are validated BEFORE any inherited verifier subprocess so the v0.4.4-owned reasons are reachable without contaminating any inherited verifier's namespace.
- **Test:** `tests/test_gold_reliance_package_index_v0_4_4.sh`. 103 exercises: 6 PP (PP1..PP6) + 6 v0.4.4-owned canonical (case49..case54 for R49..R54) + 5 runner-only refusals (ro1..ro5 covering the five v0.4.0..v0.4.3-inherited runner-only reasons) + 1 env01 INFRA diagnostic + 48 inherited canonical (case01..case48 for R01..R48) + 21 collision-class pairs (col_01_02..col_06_07 covering all C(7,2) pairs in the 7-ID class) + 5 subject-table mutations (sub01..sub05) + 5 supplementals (sup01..sup05) + 1 rel01 (inherited-relay routing) + 1 sup_det (positive determinism: subjects-and-index-fingerprint byte-identical re-run) + 1 idem01 (`--force` re-run idempotency under the v0.4.4 scratch prefix; regression-locks the macOS `/tmp` → `/private/tmp` realpath behavior in the runner's output-dir overwrite guard) + 1 no_residue (no inherited-tier residue) + 1 tg01 (closed-vocabulary token scan) + 1 ss01 (scoped byte-identical snapshot). Harness reports PASS 103/103 with top-level exit 0.
- **TG1 discipline:** closed allowlist limited to inherited-tier short-form data-field labels that share the reason-shaped suffix grammar; no v0.4.4-introduced data-field name is a substring of any v0.4.4 reason name R49..R54. The TG1 scanner's token-match regex is non-greedy with a trailing `\b` to prevent spurious capture of shorter tokens inside longer composite tokens. The TG1 DENY patterns are string-concatenated at runtime so the literal full token never appears in the scanner's own source.
- **INFRA boundary:** non-reason-shaped `INFRA: <one-line message>` on stderr followed by exit code 3, reserved for environmental failures of the v0.4.4 verifier's subprocess invocation of an inherited verifier; not in the closed reason set; not in any TG1 allowlist or DENY pattern.
- **Reason surface:** **54 = 48 inherited reasons R01..R48 + 6 v0.4.4-owned reasons R49..R54.**
- **Runner refusals (5, verbatim from v0.4.0, no sixth):** `runner_input_path_missing`, `runner_input_path_forbidden`, `runner_input_file_missing`, `runner_input_read_failed`, `runner_input_json_invalid`. Phase A preflight extends to all three inherited inputs.
- **Reference:** `docs/gold/gold-reliance-package-index-v0.4.4.md` holds the full reason surface (54 = 48 inherited reasons R01..R48 + 6 v0.4.4-owned reasons R49..R54), reachability orderings, runner and verifier subprocess-delegation architecture, the 7-ID pairwise-distinctness collision class, the TG1 allowlist discipline, the INFRA diagnostic boundary, the test scratch path policy, and the non-claims.
- **Non-claims:** Unsigned; not a new Gold tier; not a certificate; not federated; not a registry; not a federation handle; not a transfer of reliance to any external party; not a policy engine, GRC platform, gateway, SIEM, observability backend, certification authority, regulator, lifecycle adjudication authority, or production authorization system; does not consult any live service; does not re-derive or summarize any inherited subject body; does not extend the substance of the v0.4.0 body, the v0.4.1 decision report, the v0.4.2 policy-evaluation pair, or the v0.4.3 lifecycle pair; not a signed reliance instrument; not a signed lifecycle attestation; not live lifecycle adjudication; not full Gold; not Platinum; does not represent runtime truth.

---

## Next release

- **v0.4.x onward:** not yet planned. Future Gold-tier releases will extend the Minimal Gold Governed Reliance Demo surface.
